import React from "react";

function ProjectTag({ project }) {
	return (
		<span className="text-xs text-left font-fabrikat text-gray-500 uppercase">
			{project}
		</span>
	);
}

export default ProjectTag;
