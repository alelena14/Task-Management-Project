import React from "react";

function ProjectTag({ project }) {
	return (
		<span className="text-xs text-left font-fabrikat text-gray-400 uppercase">
			{project}
		</span>
	);
}

export default ProjectTag;
